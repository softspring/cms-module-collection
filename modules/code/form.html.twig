{% embed '@SfsCms/forms/modules_form_block.html.twig' with { 'name': 'attributes', 'accordion': accordion } %}
    {% block form %}
        <div class="col-12">
            {{ form_row(form.id) }}
        </div>
        <div class="col-12">
            {{ form_row(form.class) }}
        </div>
    {% endblock %}
{% endembed %}

<div class="container">
    <div class="row">{{ form_row(form.code, {'attr': {'data-content-edit-preview': 'code'}}) }}</div>
</div>
