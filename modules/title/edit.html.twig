{% import '@SfsCms/macros/modules_edit.html.twig' as module %}

{% for locale,localeField in form.title.vars.localeFields %}
    {{ module.start_link_tag_type(form.type,[
        module.link_id(form.id),
        module.link_class(form.class, 'sfs-title'),
        module.link_content(localeField, locale, true, 'title.form.title.placeholder'),
        module.enter_inserts_module('paragraph')
    ]) }}{{ localeField.vars.data|default|raw }}{{ module.end_link_tag_type(form.type) }}
    {# IMPORTANT: DO NOT LEAVE SPACES INSIDE THE DATA TO PLACEHOLDER WORK #}
{% endfor %}
