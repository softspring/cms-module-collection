{% set type = form.type.vars.data|default('h1') %}
<{{ type}} class="{{ form.class.vars.data|default('') }}" id="{{ form.id.vars.data|default('') }}"

    data-cms-module-title-type=""

    data-content-edit-id-target="moduleBlock"

    data-content-edit-class-target="moduleBlock"
    data-content-edit-class-default=""

    data-content-edit-field="title"
    contenteditable="true"
>
    {{ form.title.vars.data.es|default('Título de ejemplo') }}
</{{ type }}>