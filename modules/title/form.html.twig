<div class="container">
    <div class="row">{{ form_row(form.id) }}</div>
    <div class="row">{{ form_row(form.class) }}</div>
    <div class="row">{{ form_row(form.type, {'attr': {'data-cms-module-title-type-field': ''}}) }}</div>
    <div class="row">{{ form_row(form.title.es, {'attr': {'data-content-edit-preview': 'title'}}) }}</div>
    <div class="row">{{ form_rest(form) }}</div>
</div>